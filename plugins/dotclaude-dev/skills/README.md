# Available skills

## Completed

- `doctor/` — checks dotclaude's plugin, CLI tool, and environment variable prerequisites; offers to fix plugin gaps. CLI tools and environment variables are reported only, never installed or fixed by the skill.

## In Development

- `create-pr/` — handles PR preconditions, silent file-risk/label/title/body inference with a single combined preview loop, collects context from git commits, diff, and optionally github issues or scanner-issues
